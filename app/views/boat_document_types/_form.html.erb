<%= form_for(@boat_document_type) do |f| %>
  <% if @boat_document_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boat_document_type.errors.count, "error") %> prohibited this boat_document_type from being saved:</h2>

      <ul>
      <% @boat_document_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_field :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
