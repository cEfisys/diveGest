<%= form_for(@dive_service) do |f| %>
  <% if @dive_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dive_service.errors.count, "error") %> prohibited this dive_service from being saved:</h2>

      <ul>
      <% @dive_service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_field :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
