<%= form_for(@dive_service_member_type) do |f| %>
  <% if @dive_service_member_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dive_service_member_type.errors.count, "error") %> prohibited this dive_service_member_type from being saved:</h2>

      <ul>
      <% @dive_service_member_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_field :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
