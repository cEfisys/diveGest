<%= form_for(@certification_type) do |f| %>
  <% if @certification_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@certification_type.errors.count, "error") %> prohibited this certification_type from being saved:</h2>

      <ul>
      <% @certification_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_field :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
